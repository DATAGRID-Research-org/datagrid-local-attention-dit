[project]
name = "datagrid-local-attention-dit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.7.0",
    "diffusers>=0.33.1",
    "transformers>=4.51.3",
]
